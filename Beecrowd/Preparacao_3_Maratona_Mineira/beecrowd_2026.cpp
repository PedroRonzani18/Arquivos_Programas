#include <bits/stdc++.h>

int main()
{
    int casos, n_pacotes, capacity;
    cin >> casos;

    vector<pair<int, int>> *arr;

    for (int i = 0; i < casos; i++)
    {
        cin >> n_pacotes;

        for (int j = 0; j < n_pac : j++)
        {
            scanf("%d %d", &carry1, &carry2);
            arr.push_back(make_pair(carry1, carry2));
        }
        sort(arr.begin(), arr.end())

        for(int z=0; z < arr.size(); z++)
        {
            for(int l = z+1; l < arr.size() - 1; l++)
            {
                if(arr[z].second() + arr[l].second() >=)
            }
        }

            arr.clear();
    }
}